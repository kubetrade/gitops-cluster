apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rhsso-operator-subscription-application
spec:
  project: cluster
  source:
    repoURL: https://github.com/kubetrade/gitops-cluster.git
    targetRevision: HEAD
    path: sso
  destination:
    server: https://kubernetes.default.svc
    namespace: sso
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true